browser-use>=0.1.11  # Adjust version based on compatibility
langchain-openai>=0.0.2  # Replace with actual version if known
pydantic>=2.0.0  # Pydantic is used for the UserDetails class
python-dotenv>=1.0.0
playwright>=1.40
